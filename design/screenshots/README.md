# Screenshots

Captured from [../preview/index.html](../preview/index.html) (approximate rendering: web Roboto at
the firmware's line heights, wrapped with the firmware's advance tables; the emulator is the
truth). Named `<profile>-<orientation>-<screen>.png`. Regenerate by serving `design/` with
`node design/preview/serve.mjs` and pressing **Export all screens** in the preview, or with any
subset from the browser console via `window.quirePreview`.
